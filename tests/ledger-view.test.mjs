import test from 'node:test';import assert from 'node:assert/strict';import{World}from'../src/world.js';import{editTransfers}from'../src/ledger-view.js';
function fixture(){const w=new World(16,'ledger-view');w.kill(273);w.edit({i:136,kind:'plant',species:6,value:1});w.qi[273]=30;return w;}
test('death and removal display recorded transfer and distinct net change',()=>{for(const kind of ['death','remove']){const w=fixture();w.edit({i:136,kind,layer:1,value:0});const rows=editTransfers(w.inspect(136)).rows;assert.deepEqual(rows,[[kind==='death'?'死亡 → 遗骸':'植物 → 世界外',30],['人工净增减',kind==='death'?0:-30]]);}});
test('capacity reduction displays retained overflow, normal simulation and missing history stay distinct',()=>{const w=fixture();w.edit({i:136,kind:'trait',layer:1,field:'capacity',value:10});const d=w.inspect(136);assert.deepEqual(editTransfers(d).rows,[['承载缩减 → 遗留物',20],['人工净增减',0]]);assert.equal(editTransfers({...d,events:[]}).unknown,true);w.step();assert.equal(editTransfers(w.inspect(136)),null);});
